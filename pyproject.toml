[project]
name = "sunnydaybot"
version = "0.1.0"
description = "Daily market forecast report (Rust) published via MkDocs"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "ghp-import>=2.1.0",
    "mkdocs>=1.6.1",
]
